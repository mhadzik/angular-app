import { MenuOptionDirective } from './menu-option.directive';

describe('MenuOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuOptionDirective();
    expect(directive).toBeTruthy();
  });
});
